# Completion Log

Auto-captured by `.claude/hooks/verify-completion.sh` whenever Claude
claims a task is complete. Each entry records the timestamp, branch,
the claim word, and whether `npm run typecheck` (or your equivalent)
passed at that moment.
