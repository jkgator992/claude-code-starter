# TODOs

Auto-captured from `// TODO:` / `-- TODO:` / `# TODO:` comments by
`.claude/hooks/capture-gotcha.sh`.

Mark done by replacing `- [ ]` with `- [x]` and removing the entry
once the underlying code no longer carries the TODO.
