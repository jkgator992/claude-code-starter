# Changelog

Auto-populated by `.claude/hooks/update-changelog.sh` after each
`git commit`. Conventional-commit subjects (`feat: …`, `fix: …`, etc.)
are categorised; other subjects fall through as `chore: …`.

## Unreleased
